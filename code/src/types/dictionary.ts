export type Dictionary = {
    nav: {
        caption: {
            map: string;
            dashboard: string;
            registry: string;
            generalReg: string;
            help: string;
        },
        title: {
            map: string;
            dashboard: string;
            registry: string;
            help: string;
        },
        tooltip: {
            home: string;
            map: string;
            dashboard: string;
            registry: string;
            help: string;
        }
    };
};

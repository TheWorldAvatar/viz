export type Dictionary = {
    nav: {
        tooltip: {
            home: string;
            map: string;
            dashboard: string;
            help: string;
            registry: string;
        }
    };
};
